# Dofus 1.29 fidelity audit — confirmed findings

63-agent audit (6 dimensions, adversarially verified: a 1.29-accuracy refuter and a bible-keeper
per finding). 24 confirmed / 4 rejected. Status key: [ ] open, [x] done.

## [x] (high/formulas) Turn order does not alternate teams (pure initiative sort)
- **1.29:** 1.29 alternates teams: the team containing the highest-initiative fighter acts first, then turns strictly alternate ally/enemy, each team consuming its own fighters in descending initiative. This you-then-them rhythm is core to how a Dofus fight reads. The bible (§2.2, §6.6) says 'initiative-ordered turns ... imitate Dofus' and does not list this as a deviation.
- **Fix:** Implement as proposed in CombatEngine.Start(): sort each team by initiative descending (use a stable tie-break, e.g. original add index, so fights stay deterministic for the Track B event-log parity goal in §6.6), interleave starting with the team owning the single highest-initiative fighter, append the larger team's leftovers, and freeze that order for the fight. One adjustment for full 1.29 fidelity: CombatEngine.Summon() currently appends the summon at the end of _order, which would break the

## [x] (high/formulas) Tackle contradicts the bible's flattened rule (implements Dofus 2.x-style proportional AP+MP drain)
- **1.29:** Real 1.29 used a dodge roll: escape chance = 300*(a+25)/(a+b+50) - 100 %, clamped 10-90%; failure keeps you in place and costs the turn's points. The bible DELIBERATELY replaces this (§3.4, §6.2) with a deterministic MP-only surcharge: extra MP to leave adjacency = max(0, (enemyAGI - moverAGI)/10), i.e. a movement-cost surcharge, no AP loss, no post-move drain. The code matches neither 1.29 nor th
- **Fix:** Implement the bible rule in CombatEngine/Pathfinding: when a mover starts adjacent to one or more living enemies and its path's first step leaves that adjacency, add max(0, (maxTacklerAgi - moverAgi) / TackleDivisor) MP to the move's cost, with TackleDivisor = 10 kept as a named constant commented as the bible's "placeholder divisor". Apply the surcharge inside the cost function used by both Pathfinding.ReachableCells (so MovementRange and the AI policies/UI see true costs) and TryMove (so the c

## [ ] (medium/formulas) Critical hits fire in the hero-slice path despite the bible's 'no crits', and the crit model is not 1.29's
- **1.29:** 1.29 crits use a separate crit-damage row per spell level (not a multiplier), and the effective rate is agility-modified: N' = floor(baseN * 2.9901 / ln(Agility + 12)), capped at 1/2. Flat +50% post-flat-damage at a static 1/N matches neither the bible (which forbids crits) nor 1.29 (if crits ever return).
- **Fix:** Do option 1 now, slightly hardened: gate the crit AND critical-failure rolls off in CombatEngine (a single bible-honoring switch or outright removal of the roll block at CombatEngine.cs:281-289) rather than only zeroing SpellLibrary rows, so no future content can reintroduce crits by accident; also zero the CriticalChanceOneIn/CriticalFailureOneIn values in SpellLibrary.cs since critical failures are part of the same deferred crit system. Drop the second half of the proposal for now (bible defer

## [ ] (medium/formulas) Summons act at the end of the round, not right after their summoner
- **1.29:** In Dofus (1.29 included), a summoned creature is inserted into the timeline immediately after its summoner and plays right after them every round.
- **Fix:** Summon() currently takes only the summoned fighter and doesn't know the summoner, so thread it through: change the signature to `public void Summon(Fighter fighter, Fighter? summoner = null)`. Compute `int idx = summoner != null ? _order.IndexOf(summoner) : -1; if (idx >= 0) { _order.Insert(idx + 1, fighter); if (idx + 1 <= _pointer) _pointer++; } else { _order.Add(fighter); }`. Update the EffectKind.Summon call site (CombatEngine.cs ~line 338) to pass `caster` as the summoner. In the normal cas

## [ ] (high/spells) castsPerTarget is missing from the entire spell pipeline, and damage spells have no per-turn caps
- **1.29:** The 1.29 spell row carries BOTH 'casts per turn' and 'casts per target' as first-class limit fields, and nearly every attack spell in the Incarnam band ships with a 2-3 casts/turn or 1-2 casts/target limit (verified via dofuswiki.fandom.com Spell page search results: 'Casts per target is the number of times you are allowed to cast the spell each turn on a particular target'). Focus-fire limits are
- **Fix:** Keep the proposal but refine the plumbing: (1) Do NOT change CanCast's signature — it already receives the target cell; inside CombatEngine.CanCast resolve the occupant fighter at that cell and skip the per-target check when the cell is empty or the spell has TargetsGround (e.g. blink). (2) Track per-target casts in Fighter as Dictionary<(int spellId, string targetId), int> keyed on Fighter.Id, cleared in the existing BeginTurn alongside _castsThisTurn, and recorded in RecordCast by passing the 

## [ ] (high/spells) linear flag and area shapes exist in the engine but cannot be expressed in the Tithe skill data
- **1.29:** The 1.29 spell row includes the linear flag and an area code (cross/circle/line sizes) per level; the Incarnam band already teaches both grammars (Cra's Lashing Arrow-style linears, cross-area attacks, boss AoE). The bible §6.4 explicitly lists 'linear' and 'area (point | cross)' in the required schema, so the data schema contradicts the bible.
- **Fix:** Add 'linear': bool and 'area' to SkillDto and RankDto and map them to SpellDef.LineOnly/Area in BuildSkill (TitheContent.cs), but restrict the data-level area grammar to the bible's closed set 'point | cross' (§6.4) — e.g. compact string "cross:1", default point/Single — rather than the proposed cross|circle|line. The engine's AreaShape supports circle/line, but exposing them in the Tithe data schema would itself be a new bible deviation; widen the grammar only if the bible is amended first. Pro

## [ ] (medium/spells) Spell ranks do not behave like 1.29 spell levels: 2-3 ranks, flat 1-point cost, no damage or crit growth, no level gates
- **1.29:** 1.29 spells have 5 levels (+ level 6 at character 100+), with escalating costs — 1/2/3/4/5 points, 10 total to level 5 (verified: dofuswiki.fandom.com/wiki/Spell_Point search results) — plus minimum character levels per spell level. Per level, the dominant changes ARE damage and crit rate, often plus range, sometimes an AP drop at level 5/6 (verified: 1.29 patch notes show per-level range ladders 
- **Fix:** Keep the change with four constraints: (1) every rank row must retain an economics/shape component — damage deltas ride along (e.g. piercing_shot rank2 {"min":3,"dmgMin":+1,"dmgMax":+2}), never a damage-only row, preserving the literal §6.3 rule; (2) put the per-rank cost in the TitheTables.cs rank row itself (e.g. "cost": 1/2/3 per row, default 1) rather than a hardcoded ladder in TitheContent.cs, per the data-driven discipline; (3) scale minLevel to the slice's ~15 cap (e.g. rank3 at level 6, 

## [ ] (high/progression) Party XP split is missing the 1.29 group coefficient and the 1/3-level significance threshold
- **1.29:** 1.29/Retro multiplies the mob XP pool by a coefficient based on the number of SIGNIFICANT group members — those whose level is at least 1/3 of the highest-level member: 1 member x1, 2 x1.1, 3 x1.5, 4 x2.3, 5 x3.1, 6 x3.6, 7 x4.2, 8 x4.7. The boosted pool is then split pro-rata by level, and each member's wisdom bonus applies to their own share (the code already gets the level-weighting and per-mem
- **Fix:** As proposed, with three small hardening tweaks: (1) guard the empty/zero case — compute the coefficient only when sharers.Count > 0 (significant is then guaranteed >= 1 since the max-level sharer always satisfies Level*3 >= maxLv), and clamp the index: int idx = Math.Min(significant, coef.Length) - 1; so a future >8 crew can't throw. (2) Apply the boost to the pool BEFORE the level-weighted split and per-member wisdom (as proposed — this matches 1.29 order), and keep the boosted value as Result.

## [ ] (medium/progression) Characteristic points are spent 1:1 forever — the 1.29 soft-cost tiers the bible mandates are absent
- **1.29:** 1.29 characteristic costs are tiered per class (main stats typically 1:1 up to a breakpoint — e.g. 50 or 100 — then 2:1, 3:1, 4:1, 5:1 in successive bands; Wisdom 3:1 for every class; Vitality 1:1). With a Cra-like agility tier (1:1 to 50, 2:1 to 100), those same 56 points from base 22 buy only +42 (28 to reach 50, then 28 points = +14), landing at ~64 not 78. At the Incarnam cap that is ~14 extra
- **Fix:** Add the costTiers table to TitheTables.cs as proposed (class-agnostic: mainstat tiered e.g. [[50,1],[100,2],[150,3]], vitality [[9999,1]], wisdom [[9999,3]]), but implement the spend via the deterministic option only: derive stats purely from level inside TitheContent.StatsOf by simulating the 5-points/level spend against cumulative tier costs in a fixed order (e.g. each level, allocate points one at a time cycling stats weighted by the class growth ratio, main stat first). Do NOT add a spent-po

## [ ] (medium/progression) No level-gap XP reduction: high-level crews farm grade-1 packs at full XP forever
- **1.29:** 1.29 reduces XP when the character group's level greatly exceeds the monster group's level (the level-difference reducer: full XP when levels are comparable, sharply cut — approaching zero — once the player group is a large multiple, ~2x+, of the mob group level). This is what pushes 1.29 players out of Incarnam's starter fields and into tougher content as they level.
- **Fix:** Keep the change, with three discipline tweaks: (1) the band table must live in TitheTables.cs as a JSON table (e.g. "xp_level_gap_bands": [{"ratio": 1.5, "pct": 100}, {"ratio": 2.0, "pct": 70}, {"ratio": 3.0, "pct": 30}, {"ratio": null, "pct": 10}]) with a "placeholder pending §9 mining" source comment — never as constants in TitheResolution.cs. (2) Apply the reducer to the pool BEFORE the level-weighted split and per-member Wisdom bonus (matching 1.29's order: mob-group XP is reduced by the lev

## [ ] (high/items) Adventurer Set has 7 pieces including a weapon; the real 1.29 set is 6 pieces with no weapon
- **1.29:** The real Panoplie Aventurier is exactly 6 items: Amulette, Anneau, Bottes, Cape, Ceinture, Coiffe (amulet, ring, boots, cape, belt, hat), all ~level 1-6, droppable and craftable. It contains NO weapon; 1.29 starter sets and weapons are separate chases (confirmed via dofuswiki.fandom / solomonk search results).
- **Fix:** In TitheTables.cs: (1) delete the adv_blade row outright rather than making it set-less — TitheContent.UniqueIds() defines uniques as Set == null, so a set-less adv_blade would silently join the behavior-rule uniques pool (Gravewalkers/Piper's Whistle) and claim the ~15% screaming-find channel in RandomUnownedGear, violating the bible's unique idiom; if you want to keep it as a standalone weapon instead, add an explicit "unique": true data flag (or filter UniqueIds on Ap/Mp != 0) so it stays out

## [ ] (high/items) Item stats are fixed; 1.29 items roll min-max ranges (jets), and duplicate drops are silently discarded so there is no reroll chase
- **1.29:** Every 1.29 item stat line is a rolled range — the Adventurer pieces themselves are '1 to 2' in each of five stats (verified via wiki snippets). Chasing a perfect jet, and re-dropping a piece hoping for a better roll, is a core part of how 1.29 loot feels; duplicates are also the vendor/market economy.
- **Fix:** Adopt the change with four constraints: (1) In ItemsJson (TitheTables.cs), allow each stat as either a scalar (fixed) or [min,max] (rolled); keep the uniques' behavior lines fixed scalars — "mp": 1 on gravewalkers and "ap": 1 on pipers_whistle must never roll, matching 1.29 where AP/MP item lines are fixed and matching the bible's "behavior-rule unique" identity. SetsJson tier bonuses stay fixed (1.29 panoply bonuses don't roll). (2) Roll at drop time in TitheContent using the campaign's IRng (b

## [ ] (high/items) Per-piece stats are chunky and asymmetric; real Adventurer pieces are uniformly tiny (1-2 in five stats, no Vitality, no Power)
- **1.29:** Each of the 6 real pieces gives +1~2 Wisdom, +1~2 Strength, +1~2 Intelligence, +1~2 Chance, +1~2 Agility — identical junk-tier spreads on every piece, zero Vitality, and no Power stat (Puissance never appears on 1.29 starter gear). The set's whole identity is 'pieces are worthless, the completed panoply is everything'.
- **Fix:** Flatten the seven adv_* piece rows in TitheTables.cs ItemsJson to the mined 1.29 spread: 1-2 each in Wisdom, Strength, Intelligence, Chance, Agility; remove all vitality and power keys from pieces. Prefer the true mined 1-2 values over the '2-4 faster pacing' variant — the bible says 'mined values as reference'; only escalate to 2-4 if a sim run (dotnet run --project DofusSlice.Sim campaign survey 40) shows early-campaign survival/TTK regressing unacceptably after the Vitality moves off pieces. 

## [ ] (medium/items) Set-bonus tiers are Vitality-led and too small; real full-set bonus is ~+40 in ALL five stats plus +40 Vitality (the +1 MP at full set is CORRECT)
- **1.29:** Full-set totals of 46~52 in each of Wisdom/Strength/Intelligence/Chance/Agility plus 40 Vitality (wiki snippets) minus the pieces' 6~12 imply a 6/6 bonus of about +40 in each of the five stats and +40 Vitality — an absurdly oversized bonus for level-6 gear (why wisdom-maged Adventurer sets were the retro leveling meta). The famous full-set +1 MP is real (model knowledge, corroborated by one search
- **Fix:** Apply the proposed tier curve, but reconcile the piece count first: the coded set has 7 pieces (adv_blade weapon + 6 armor, TitheTables.cs lines 234-240, tiers 2..7, mp at 7), while the bible §5/§6.10 says six pieces with 2..6 tiers — and the real 1.29 Adventurer set has no weapon. Preferred: remove adv_blade from the set (keep it as a plain weapon or cut it), use tiers exactly 2pc +5 all / 3pc +10 / 4pc +18 / 5pc +28 / 6pc +40 all + 40 vit + mp:1. If the 7-piece slice set is intentionally kept,

## [ ] (high/bestiary) Grade scaling is a steep flat multiplier instead of 1.29's authored per-grade rows (+30% HP/step is 2-4x real)
- **1.29:** Real 1.29 mobs have 5 hand-authored grade rows, each with its own level (+1 per grade), HP, initiative, stats and XP. Per-step HP growth in the Incarnam/Chafer band is roughly +8-15% (e.g. Gobball ~35->55 HP over grades L6->L10, Chafer ~90->110 over L36->L40), initiative grows with the row, and AP/MP are per-row (occasionally +1 at top grades). A grade-5 mob is ~1.2-1.6x the grade-1 HP, not the 1.
- **Fix:** Prefer the primary option: author per-grade rows in MobsJson inside TitheTables.cs (per-grade level, HP, AP, MP, initiative, stats, XP, gold), tuned to the mined 1.29 Incarnam/Chafer bands (~+8-15% HP per step, grade-5 at ~1.2-1.6x grade-1 HP, initiative growing per row, level +1 per step). Store the real level in Fighter.Level and carry the grade in a separate field (e.g. Fighter.Grade or an int passed alongside) so MobXpOf/MobGoldOf in TitheContent.cs (lines 255-257) stop reading grade out of 

## [ ] (high/bestiary) Aggro is a single global 6-cell radius on a per-pack boolean, contradicting the bible's per-type radius (and 1.29's per-monster aggro)
- **1.29:** 1.29 aggression is a per-monster-type radius: each aggressive monster type has its own trigger distance in map cells, passive types have none, and walking inside the radius starts the fight regardless of what you just did. The bible §5 bestiary table itself assigns per-type tiers (Gravehound 'Very wide', Grave Mites 'Medium', Marrow Spitter 'Narrow', Barrow Husk 'Near zero', Crypt Warden 'Zero unt
- **Fix:** Add per-type "aggroRadius" to MobsJson in TitheTables.cs (placeholder mapping of §5 tiers: gravehound 8 "Very wide", grave_mite 4 "Medium", marrow_spitter 2 "Narrow", bone_piper 2 "Narrow", barrow_husk 1 "Near zero", crypt_warden 0 "Zero until approached"), commented as M5-tunable placeholders. In TitheContent, compute PackDef.HuntRadius = max aggroRadius over the pack's comp and keep Hunts as a derived property (HuntRadius > 0) so existing DiveSession.RollHunters and SliceGame p.Def.Hunts check

## [ ] (medium/bestiary) Packs are 6 fixed hand-authored comps with no size range, no solo groups, and a uniform grade per pack
- **1.29:** 1.29 zone groups are rolled from a spawn table: 1-8 monsters, randomized composition per respawn, and grades mixed within a single group (one group shows a lvl 36 and a lvl 40 Chafer side by side). Lone/small groups are essential — early play is picking off the single Arachnee. The bible §6.7 itself specifies 'mob type, pack size range, count' spawn tables, so fixed comps contradict it. (Pack ceil
- **Fix:** Keep the proposal, with these adjustments: (1) Schema in GraveyardJson per pack slot: { "id", "pool": [mobIds], "size": [min,max], "reach", "hunts", "grade" } — grade stays the slot's base grade; keep at least one near slot with pool [barrow_husk, marrow_spitter] and size [1,2] as the guaranteed lone-Arachnee-style farm target (guarantee it in data, not code). (2) Roll in the DiveSession constructor using the session's IRng (dive start == respawn between dives, per bible; also keeps rolls seed-d

## [ ] (medium/bestiary) Every mob in the bestiary has exactly 6 AP (boss 8) — 1.29's low band runs 3-5 AP with real variance
- **1.29:** In the Incarnam/Astrub/Chafer band, monster AP is authored per row and mostly lower and varied: Tofu/Arachnee/Gobball-tier mobs run ~3-5 AP (often one attack plus movement), with 6+ AP reserved for elites and bosses. Uniform 6 AP makes the whole bestiary act like elites and erases the per-family action-economy identity 1.29 mobs have.
- **Fix:** Edit only TitheTables.cs mob rows: grave_mite 4, barrow_husk 4, marrow_spitter 4, bone_piper 4, gravehound 5, crypt_warden 5 (sexton already 8). Compensate the halved cast count (e.g. husk went from 2x husk_strike to 1x) via the data tables only — raise the affected mobs' strength or skill min/max rows, not code. Prefer per-family compensation so identity sharpens: husk hits harder per swing (slow heavy melee), mite keeps low damage (its job is Sap PM-drain), spitter/piper mostly fine as one-cas

## [ ] (medium/bestiary) The skeleton family has almost no elemental identity — no family-wide resists and no weaknesses (negative resists) anywhere
- **1.29:** 1.29's Chafer/undead family reads as a family: bony mobs share meaningful neutral/earth resistance and exploitable elemental weaknesses (negative resistances existed and mattered — picking the element the family is weak to is core 1.29 play). A graveyard where the Fire Cannon, Earth Bulwark and Air Archer all hit every skeleton identically loses that element-matchup texture.
- **Fix:** Apply as data-only edits to MobsJson in TitheTables.cs, respecting the rows that already have profiles: add modest resEarth (10-20) plus resFire -10 to -20 to barrow_husk, marrow_spitter and grave_mite (and optionally bone_piper at the low end); leave crypt_warden (resEarth 35/resFire 15 — the family's armored peak, deliberately NOT fire-weak) and sexton (20 all) as-is, and leave gravehound (a hound, not bony) earth-neutral so the family reads as skeletons-specific. No formula change is needed —

## [x] (high/presentation) Floating damage numbers are one uniform red instead of element-colored
- **1.29:** 1.29 floats the damage number over the target in the ELEMENT's color: Neutral grey/white, Earth brown-ochre, Fire red-orange, Water blue, Air green; heals float green. Since TITHE's classes are element-keyed (Cannon=Fire, Archer=Air, Bulwark=Earth per the comment at SliceGame.cs:1854-1857), element-colored numbers instantly make each class's hits read differently, exactly the 1.29 feel.
- **Fix:** As proposed, but avoid a third duplicated element-color switch: the repo already has two divergent ones (BattleAnimator.SpellColor at BattleAnimator.cs:201 and SliceGame.ElementColor at SliceGame.cs:1753). Extract a single shared internal static Color ElementColor(Element e) helper (e.g. on BattleAnimator), have SpellColor and SliceGame delegate to it, and have HitAnim use it for damage floaters. Pass d.Element at the damage call site (BattleAnimator.cs:66); leave the heal call site (line 69) on

## [ ] (high/presentation) Placement phase shows only the player's blue cells — 1.29 shows BOTH teams' start cells, red vs blue
- **1.29:** The 1.29 preparation phase tints the start cells of BOTH teams — one team red, one team blue — visible to everyone; you place onto your own team's color. (Team-colored placement cells confirmed via dofuswiki Combat page; the red=challenger / blue=defender assignment is model knowledge.)
- **Fix:** In DofusSlice.Game/Rendering/Palette.cs, next to PlacementCell add: `public static readonly Color PlacementCellEnemy = new(214, 58, 48, 150); // enemy start cells, 1.29 red`. In SliceGame.cs DrawPlacementCells(), after the blue loop and before the hover outline, add: `foreach (var f in _engine.Fighters.Where(f => f.Team != Team.Player)) _prim.DiamondAt(_sb, _proj.CellCenter(f.Pos), Palette.PlacementCellEnemy);`. Do NOT implement the optional crew-red/enemy-blue swap — keep crew=blue (matching th

## [ ] (medium/presentation) Turn order is a horizontal Dofus-2-style card strip; 1.29 used a vertical fighter list with blue ally / red enemy names
- **1.29:** 1.29's turn system read from a vertical fighters list on the right edge: rows in initiative order, ally names in blue, enemy names in red, current fighter highlighted (orange), dead fighters greyed/struck. There was no horizontal portrait strip in 1.29.
- **Fix:** Same change with three corrections: (1) The "WATCHING — NAME" header is at SliceGame.cs:1532-1533 (DrawTitheHud), not 1364-1365; recolor it there, and also recolor the parallel piloted-mode header "{NAME}'S TURN" at SliceGame.cs:1503-1504 (DrawHud), since DrawTurnTimeline is shared by both HUDs — replace Palette.HpFill/Palette.EnemyColor with ally blue (90,150,240) / enemy red (220,70,60). (2) Avoid overlap with the combat log: the vertical stack at x = ScreenW-20-cardW = 1164 collides with log 

## [ ] (medium/presentation) Watched combat never shows the green movement-range cells on a fighter's turn
- **1.29:** In 1.29, the moment a fighter's turn starts, its reachable cells tint green (the PM zone) before it moves. The bible forbids piloting input, not display: Pillar 2 ('fully legible') actively wants this, and drawing the zone adds zero input paths.
- **Fix:** In DrawFloorOverlays (SliceGame.cs ~line 1159), keep the CurrentRing draw, then replace `if (_tithe) return;` with a display-only branch: when `_tithe && !_anim.IsBusy && _engine.Outcome == FightOutcome.Ongoing && _engine.Current.IsAlive`, draw the active fighter's reachable cells with Palette.MoveRange. Do NOT call _engine.MovementRange every Draw frame as the proposal implies — cache it once per active-fighter/turn change (e.g., recompute where the turn advances, or memoize keyed on the Curren

## [ ] (medium/presentation) Placement commit button says 'FIGHT!' in green instead of the 1.29 orange READY button
- **1.29:** 1.29's preparation phase puts an orange 'Ready' (Prêt) button exactly where the End Turn button lives during the fight; clicking Ready is how a fight starts. The button being orange and labeled Ready is one of the most recognizable 1.29 fight-screen beats.
- **Fix:** In DofusSlice.Game/SliceGame.cs: (1) line 1236 change the label "FIGHT!" to "READY" (current label has no "(SPACE)" suffix — either keep it bare or add "(SPACE)" at smaller scale; the SPACE hint already lives in the helper text); (2) line 1234 area: stroke the button rect in Dofus orange (e.g. new Color(235,140,40)) and use Palette.HudPanelLight fill on hover as proposed — consider an orange-tinted fill (e.g. new Color(235,140,40) at reduced alpha or a darker orange) so the button reads orange l

## Rejected (deliberate bible deviations, kept)
- (formulas) Pushback collision damage: 5/cell and no damage to the blocking unit (bible says 4/cell, half to blo
- (spells) Piercing Shot costs 4 AP in data but the bible specifies 3 PA
- (spells) MP drain lands 100% of the time — no Wisdom-based AP/MP-loss dodge on the status path
- (items) Drops ignore prospecting; the Chance stat is inert for loot